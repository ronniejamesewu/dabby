# Dabby — Backlog

Open items, deferred work, and completed one-liners (index only — full
history in git).

---

## Open

- **SessionStart hook to enforce required file reads** — CLAUDE.md instructions alone have failed in Sessions 15, 27, 94, 95, 100, and 101. Session 98 partial fix: CLAUDE.md restructured to split session-start reads into always-reads and conditional reads. Session 101: mandatory reads moved to a blockquoted gate at the top of CLAUDE.md, before any project context, with explicit "do not respond first and read later" language. Hook approach evaluated in depth Session 101 and deferred — downsides (staleness within session from one-time injection, double reads if CLAUDE.md still triggers Read calls, unconditional context loading on every session, Windows/PowerShell platform friction, invisible injection making debugging harder) outweigh the benefit over a stronger CLAUDE.md gate. If the Session 101 gate still fails, revisit the hook — the evaluation and tradeoffs are documented here. Session 141 update: the dab skill is now the primary mechanism — it triggers on dab intent (the exact condition under which the gate historically failed) and sequences the reads, with `/dab` as a deterministic manual path; the CLAUDE.md gate remains the backstop for non-dab sessions. Revisit the hook only if skill triggering proves unreliable in practice.

- **Dabby as open-source companion spec** — Two related but distinct artifacts: (1) **Open-source the full project** (generator, data model, website) with personal run data stripped — standard open-source playbook, well-understood, do it when the project feels stable enough. (2) **A portable companion spec** — the more interesting and harder thing, raised Session 94 while high and thinking clearly about it. The value isn't the logging tool; it's the *companion experience*: Claude coached to be genuinely useful in this domain, opinionated, epistemically honest, with a point of view on what the signal means and what it doesn't. That lives in the instructions layer (CLAUDE.md, Handoff_Notes, Handoff_Wisdom, Methodology) and is abstractable without losing personality. Design principles for the abstraction: **keep the conclusions, strip the citations** ("swab is a floor indicator, not a calibration metric" stays; "as seen in FW106 R11-12" goes); **preserve the opinions unapologetically** — this isn't a neutral framework, it's a strongly opinionated companion spec and the specificity is the product; **don't sand down the personality in the process of genericizing**. The harder design challenge is the *first session* — a stranger arrives with no run history, Claude needs to onboard gracefully (ask about strain and rig, explain what it'll track and why, suggest the baseline curve, start building from run 1). The methodology documents give enough to be useful immediately. Separate from the open-source work; could be done independently as a `companion-spec` branch or separate repo. **Known cuts and degradations when shipping publicly:** (a) *Equipment scoping* — scope explicitly to programmable setpoint devices (electronic rigs with repeatable curves); torch rigs have four uncontrolled variables where this project has one, and run-to-run comparisons don't build anything meaningful without controlled input. Exception path: torch users with an IR thermometer logging peak and drop temp on every run — note it as a way in, not an expectation. This is a deliberate scoping decision, not a limitation to apologize for. (b) *Evidence-trail degradation* — every methodology position in the wisdom layer has calibrated confidence backed by specific run citations; strip the citations and the public version ships assertions without receipts. Stranger gets the conclusion, not the earned weight behind it. The onboarding framing needs to acknowledge this: "these positions are starting priors, your runs will confirm or revise them" — not presented as settled truth. (c) *The day-1 gap is real and shouldn't be papered over* — the companion experience compounds with history; the first few sessions are genuinely thinner. Don't fake depth that isn't there yet. The self-excising onboarding idea (Claude writes a blank-slate HANDOFF_STATE and stripped CLAUDE.md at end of onboarding, then removes the onboarding section) is worth exploring as an honest acknowledgment of this rather than pretending day 1 is session 94.

- **Session date backfill** — CAG Run 1 and OC Runs 1–3 have `run_date = None`. Update if user can recall the dates.

- **Quantify "rice grain" load descriptor** — weigh a few loads to establish a mg range (e.g. 0.05–0.15g). One-time calibration; update the global constants with the range.
- **Control water temperature and change frequency as variables** — standardize practice and log it.
- **Visual distinction between What to Try Next and new-strain onboarding** — the two contexts (continuing calibration vs. opening a new strain) look identical but carry different intent.
- **Workflow simplification** — Make the PR workflow optional for routine run logging. Update CLAUDE.md so the standard session ends with a local `index.html` review and direct commit to main; PR stays the right path for refactors, UI changes, anything where the preview URL adds value. Key constraint: any local-first path needs a mandatory rendered-diff review gate with teeth — the PR preview is also the content-loss tripwire, given two logged silent-content-loss incidents.
- **CLAUDE.md User Configuration block** — Extract timezone, device, insert type, and technique from the protocol body into a User Configuration section at the top of CLAUDE.md. A new user edits that section without touching the protocol. Prerequisite for the open-source companion spec.

- **Draw velocity and cap-pearl dynamics** — FW106 R19 introduced draw effort as an uncontrolled variable: harder draws pull more airflow → more convective cooling of pearl and insert → faster within-draw heat shed; harder draws also spin the pearls faster via the Cloud Vortex cap. The two effects partially offset (faster spin = more heat delivery per contact; more airflow = more cooling). The deeper thread: pearl thermal mass × insert circumference × spin velocity × draw velocity all interact to determine heat-per-draw delivered to material. FW106 R20 (Rig 6, Wym Stick Piston, no pearls) added first direct data: joystick delivered significantly more intensity than Rig 5 spinner at same endpoint, with harshness arriving one draw earlier — consistent with directed airflow delivering more material per draw than the vortex/pearl setup. Stationary-pearls-with-Piston (thermal mass present, spin absent) identified as a potential future isolation to separate pearl thermal mass from cap airflow design. Sessions 118, 120.

- **IRB sub-agent for What to Try Next review** — A sub-agent with its own context and a participant-welfare brief (not data-quality optimization) could serve as an external check on recommendations that propose changing protective behaviors. Trigger: only when What to Try Next proposes changing something from the prior run's technique. The startup file (passed as sub-agent prompt) should define green/yellow/red and the specific behaviors to protect; CLAUDE.md gets a small procedural note. Deferred: overhead per run for a relatively infrequent conflict. Revisit if the researcher-participant failure mode recurs and the behavioral fix (options 2+3 above) doesn't hold. Session 127.

- **Dab rig vs. erig temperature ranges — Oliver consult** — Higher-temp-hits-harder anecdote confirmed on papzp22 R6 (430°F, June 22, 2026). Questions for Oliver: (1) Have you watched the thermometer during a dab — what can you tell me about what it does? (2) Is there a sweet spot on the thermometer or on a timer where you get what you think is the tastiest part? (3) What experiments have you run? (4) What dialing-in process do you do when you change rig configuration, if any? Broader discussion: what dab rig setpoint conventions mean when translated to erig operating ranges, and whether "higher temp" anecdotes from traditional rigs carry over. Session 127.

- **Percolation glass as a testable harshness variable** — Hive #1 R6 water-reset observation (Session 123) raises the hypothesis that more percolating glass would filter particles before they reach the airway, potentially delaying harshness onset. Testable: same rig, same curve, more percolation glass piece, compare harshness onset timing. Tradeoff: more percolation also strips desirable compounds alongside particles.

- **Strain-to-strain temperature variance on Rig 6** — FW106 R21–22 mapped 420°F (wispy, light) vs. 425°F (dense, very hard) on Rig 6. User hypothesis: working endpoint doesn't shift dramatically across strains on a given rig, so this calibration informs starting points for new strains on Rig 6. Have a conversation about whether FW106's endpoint map should shape the Rig 6 baseline specifically, or whether per-strain calibration from BASELINE_CURVE remains the right approach regardless. Session 129.

- **Jar-return check** — when returning to a jar after an extended gap, flag it in pre-run discussion and suggest a lighter first load. Four implementation options explored (principle-only, named signal, wisdom-entry pattern, pre-run framing guidance); see `jar_return_check.md`. Lean toward Option C or D — avoid a mechanical protocol gate. Originated from bp4rw13 Run 5 (June 30, 2026).

- **Check selected model at dab-skill start; confirm with user if above Sonnet** — requested July 3, 2026 (Session 145), immediately after switching this session from Fable 5 to Sonnet 5 mid-session via `/model`. The dab skill's normal-mode workflow has no step that surfaces which model is running. Add a check early in the sequence (alongside or near step 3's mandatory reads) that reads the active model and, if it's above Sonnet-class (e.g. Opus, Fable/Mythos), asks the user to confirm that's intended before proceeding — the skill audience is documented as Sonnet-class sessions (see the Skill Library backlog item below), so a pricier model running unnoticed is worth a cheap confirmation. Needs a decision on mechanism: whether the harness exposes the current model identifier to a skill step programmatically, or whether this has to be a prose instruction the assistant self-reports against (weaker — same class of problem the mechanize-the-floor principle argues against). Scope to the dab skill first; consider whether log-run and new-jar want the same check.

- **Fix the pre-send register-leak check's syntactic blind spot** — surfaced July 3, 2026 (Session 145): a chat reply referenced "the swab field" — a raw schema field name in plain, unformatted English — which the dab/log-run skills' pre-send check didn't catch, because that check only pattern-matches on formatting (backticks, snake_case, ALL_CAPS, jar slugs, step labels), not on meaning. See `register_leak_diagnostic_exercise.md` (and its gated answer, `register_leak_diagnostic_answer.md`) for the full incident, a blinded diagnostic exercise for a future Claude to work through independently, and a suggested fix direction (a semantic net alongside the syntactic one — flagging any plain-English reference to a schema concept as a field/record/entry, not just formatted-looking leaks). Open question the exercise leaves unresolved: whether this can be made mechanical at all or has to stay a judgment-layer check like several entries in the Live Failure Modes section above.

- **Skill library — deferred second pass (July 2, 2026).** Design principles in `.claude/skills/README.md` and CLAUDE.md Skills section. Shipped: (1) correct-frozen-data, (3) close-jar, (4) BASELINE_CURVE-in-RUNS preflight ban, (5a) new-rig, (5c) change-baseline — Session 147; (6) CLAUDE.md integration pass, (7) skills README — Session 148. Remaining: (2) **session-close-handoff skill** — harness for the session-close checklist at the top of `WISDOM_BRIEF.md` footer plus the close steps `CLAUDE.md` describes (build spec below). (5b) **debug-deploy skill** — pipeline verification order before blaming propagation.

- **Build spec — session-close-handoff skill (item 2 above), enriched July 7, 2026 (Session 151).** Motivation: the checklist and its close steps are AI-followed prose today, and the "offer the checklist the moment a checklist question resolves yes mid-session" rule is judgment-layer — it failed in Session 145 and again in Session 151 (see Known Claude Failure Modes in `Dabby_Handoff_Notes.md`), so the *offer* itself needs mechanizing rather than trusting the AI to notice. Desired behavior (user-specified): (a) **offer the checklist at PR-creation time in the run-logging flow** — the natural session-close boundary; (b) the offer **decays in terseness** — full offer at the session's first logging PR, a terser nudge at the second, a one-line nudge on each subsequent one; (c) **suppressible** — if the user declines ("not tonight"), stop offering for the rest of the session. Scope note: opening a PR is *not* by itself the trigger — only run-logging PRs and explicit handoff-update requests are; a routine infra PR shouldn't nag (confirmed Session 151 against a mis-remembered "offer on every PR" rule that was never committed). Open mechanism question (same class as the dab-skill model-check and the register-leak items): whether the harness can fire this at PR-creation programmatically (a real gate) or whether it stays a prose instruction the assistant self-reports against — the weaker option, and the exact "trust the AI to notice" problem this item exists to fix. The skill still needs to wrap the seven-question checklist plus the header-date / regenerate / bundle-onto-open-PR close steps.

- **Lifecycle flag on `StrainStatus` — deferred, specific trigger required.** Closing a jar is a two-file edit: move the slug in `jar_manifest.py` AND update prose framing in the jar file. `_check_closed_tier()` only fires *after* the slug is already in `CLOSED` — it catches forgot-the-prose but not forgot-the-manifest-move. The manifest move is unenforced in the direction that can actually fail silently. The session-close checklist (Q7) now covers this operationally. A structural fix would be an `is_closed` flag on `StrainStatus` — jar-close becomes a one-file edit, `_check_closed_tier()` becomes an in-jar invariant, the gap disappears. Deferred because it requires changing `Dabby_Core.py`, all ~17 jar files, the manifest, and the generator simultaneously with no test suite. **Trigger:** a session where the manifest move is actually forgotten and causes a problem that Q7 didn't catch.

- **Pre-dab tasting-notes / flavor prediction in the session-open brief** — requested July 10, 2026 (Session 153). The user liked the genetics-inferred flavor note offered before the LunarZ Run 1 dab (shared Moonbow/Zkittlez ancestry with Rainbow Belts → watch for Z-forward candy character). Make it a recurring pre-dab element: a short "what you might taste / notice" line drawn from the jar's genetics and terpene inference, surfaced alongside the plan on file. Design constraints: keep it epistemically honest — inferred from genetics, not measured; nose is a weak secondary signal; the generic cannabis palette is not strain-specific knowledge (CLAUDE.md Epistemic Flags) — and keep it a passing note, not a prediction to be scored against. Open question: whether this is a `pending_dab.py brief` addition (mechanical, composing from a jar-level field) or a dab-skill prose step (judgment-layer). The jar files already carry `terpene_note` and a `Nose` info row — a brief-level version could compose from those.

- **Curve schema: Switch profile-slot color code** — requested July 10, 2026 (Session 153). The Switch² stores custom profiles in slots and the device UI assigns each a color; the user identifies curves by that color (orange = the 440°F / 10s-hold gentle descent, blue = `BASELINE_CURVE`, purple = the 430°F fast ramp, teal = the 430°F / 10s-hold bounded-hold descent — noted July 11, 2026, distinct from the purple 430°F fast ramp). Add a field to the curve schema encoding which device slot-color a given curve lives in, so the log can track/display the physical device assignment and a readback can say "load the orange profile" instead of reciting waypoints. Design questions: where the field lives (waypoint constants are bare `list`s today — this likely wants a small Curve wrapper or a lookup table keyed by curve constant), how many slots the device exposes, and whether the mapping is stable enough to hardcode or should be user-editable. Value: closes the gap between the logged curve and the button the user actually presses. **Placement idea — to be tested, not a decision (July 12, 2026):** One design direction to weigh, not a settled call: treat the mapping as curve-attribute data — not state, cross-strain pattern, or protocol — and put it on the curve in `Dabby_Core.py` alongside the `BASELINE_*` constants (device-global reference, same shelf as `GLOBAL_INFO`'s Device & Session Constants) rather than hand-maintaining it in any of the three session-start handoff docs, on the theory that a hand-kept copy would rot and fork a second source of truth the first time a slot is reassigned. The appeal of doing it this way is that it *would* also answer the "how does it surface at session-open" question for free — `HANDOFF_STATE.md` is regenerated every session and already prints each strain's plan-on-file curve, so the generator could append the slot-color to that line ("…the 430°F bounded-hold descent (teal)") with no extra upkeep, and `pending_dab.py brief` prints the same curve so the readback could say "load teal" instead of reciting four waypoints. Interim option, if the mapping is wanted visible before any schema work: hang a literal table off the Device & Session Constants reference block rather than the core handoff files. None of this is validated — it's a direction to try, and the design questions above still need answering first. **Device slot facts (July 13, 2026):** the device exposes exactly **five** profile slots, now all assigned — orange, blue, purple, teal, and **grey** (grey = the 450°F bounded-hold + shorter descent, `SOURTANGIE_HOLD_DESCENT_450`; the user first read the swatch as "charcoal," but the device just shows grey and does **not** allow custom names — the color *is* the only handle). Two consequences for the schema question: (1) the mapping is **mutable by eviction** — a sixth curve doesn't get a sixth color, it replaces one of the five — which argues against a hardcoded color→curve table (it rots on the first reassignment) and toward a user-editable mapping or a cheap-to-update lookup; (2) "how many slots the device exposes" is now answered: five. **Slot reassignment (July 19, 2026, Session 171):** purple was reassigned to the 430°F 15s extended-hold descent (`LUNARZ_HOLD15_DESCENT_430`), evicting the 430°F fast ramp — the first logged instance of the "mutable by eviction" mechanism this item predicted. Confirms a color→curve mapping in the schema must handle reassignment, not assume stability.

- **Strain effect-character field ("yoga strain") in the schema** — requested July 12, 2026. The user characterizes strains by functional effect — Sour Tangie is "a yoga strain" (energetic, functional, daytime), as opposed to a sedating/couch-lock strain. Nothing in the schema captures this axis today: `intensity` (`CompletedRun`) is *magnitude* (light/moderate/strong), and effect *character* only lives in prose (`session_char`, `analysis`). Add a field encoding the qualitative effect character so it's queryable and renderable — e.g. an energizing↔sedating descriptor with room for the user's own coinages ("yoga strain"). Design questions: (1) per-run (`CompletedRun`, this session's read) vs strain-level (`StrainStatus`, accumulated character) vs both — mirroring how `intensity` is per-run while a strain's character emerges across sessions; (2) free text vs a small controlled vocabulary; (3) how it renders (strain header, run line). Epistemic constraint: effect character is subjective and self-reported, unfolds over hours, and single-session reads are weak signal (same calibration as `intensity` and nose) — the field records the user's phenomenological read, not a clinical claim. Value: captures the "what is this strain for / how does it land" axis the log currently can't surface or compare across jars.

- **Load class into the verbatim record at logging** — surfaced by the July 11, 2026 wisdom audit. Load size is the log's most confound-cited variable, yet it repeatedly survives only in AI-authored fields (`analysis`, `endpoint_note`) rather than the user's `dab_notes` — found in at least 5 runs across 4 jars (FW106 R26/R27/R29, bp4rw13 R5, WW Z R8/R9, Hive1 R6/R8). When a wisdom row later cites "larger load" for these runs, it's citing an AI interpretation, not the user's observation — and one run (FW106 R27) had no load record in any field at all, its "larger load" label existing only inside the sibling-run comparison prose that used it. Cheap fix at the source: the log-run skill's Beat 1 readback should echo the load class the user gave into the `dab_notes` verbatim capture (or explicitly record "load not stated" when they don't give one), so it lands in the primary record rather than being reconstructed into an AI field. Applied ad hoc in the LHBH R8 and Sour Tangie R4 next-run plans as of the audit; this item is to mechanize it in the skill. **Scope corrected July 16, 2026 (Session 167) — the "at least 5 runs" figure was a sample, not a sweep, and undercounts by ~7x.** A mechanical pass over `load_all_jars()` measures it: of the 126 runs carrying `dab_notes`, **70 say nothing about load at all**, and **37 of those 70 have a load claim in an AI-authored field anyway** — i.e. on 37 runs the log asserts a load the user never stated, reconstructed from context by a model. The remaining 33 have no load record anywhere. Coverage, not vocabulary, is the whole gap (see the load-normalization item below for why an enum would make this worse, not better). Incidental confirmation of the **Mechanical run-facts dump script** item: three queries over the jar files re-derived this in minutes, against twenty extraction workers that sampled and got it wrong.

- **Load recording — vocabulary asked and answered (no); the cross-strain anchor question is open** *(July 16, 2026, Session 167)*. User asked whether load sizes should get a normalized vocabulary "similar to the color coding we use for swabs." Investigated against the code and the corpus. Answer: no — and the reason is worth keeping, because the premise doesn't survive contact with the code. **The swab field isn't normalized either.** It is `swab: str = ""` — free text, validated only for non-emptiness, with 100+ distinct logged values ranging from `golden` to `Light golden — "lightly toasted marshmallow."` to `dark amber, dark-BBQ-sauce tint`. What makes swab trustworthy is three things, none of them a controlled vocabulary: (1) a validator that refuses an empty swab; (2) the protocol rule "do not log without the swab" — cheap to honor because the swab is the standard insert-cleaning step, so the measurement is free; (3) the Swab Color Reference, which is a *meaning* ladder (target / too hot / too hot severe / too cool), not a word list. Load has none of the three. That gap is what the **Load class into the verbatim record at logging** item above targets, and it is the whole of the problem.
  - **Why an enum would actively hurt.** Every load phrase in the corpus carries an epistemic hedge — "I *think* I probably loaded less rather than more," "I *suspect* I loaded a light load without meaning to," "load *normalish*, maybe a tad bigger," "Load was slightly larger than normal **or so I thought, we know this part is unreliable**." The words already cluster fine (normal / larger / smaller / light / big); what they carry *on top* is the user's own confidence, and the user already knows the estimate is unreliable — they say so, unprompted, in the record. Collapsing that to `load: normal` deletes the hedge and reports an estimate as a quantity: invented specificity mechanized into the schema, in well-formed shape no validator can catch (structurally identical to the bamboo entry's fabricated confound passing its non-empty check by *starting with* "None noted —"). Swab escapes this because it names a physical artifact observed after the fact; load is an estimate of a target that was never hit, on material that resists portioning — already ruled a property of the jar, not a technique gap (bp4rw13, Session 138).
  - **The open thread, and it's the real one.** The Swab Color Reference opens: *"Swab color is a qualitative directional signal within a strain. Do not compare across strains — starting material color, oxidation state, and terpene-to-cannabinoid ratio all affect residue color independently of temperature."* The `load-size-effects` wisdom entry aggregates load claims across **six strains** and concludes the direction is "split and unresolved." But "normal" is a per-jar anchor, and the user's own words say so: *"this jar is runnier than the other thumbprint jars," "consistency made it hard to load a normal amount."* A runny thumbprint's normal and a stiff badder's normal are different masses wearing the same word. So `load-size-effects` may be doing to load precisely what the swab reference forbids for swab, for the same reason — and "split and unresolved across six strains" is what summing incommensurable units looks like. **The split may not be a finding; it may be the method.** Task: pull the entry's 16 citations, check per-jar whether "larger/smaller" is anchored within-jar, and determine whether the split is an aggregation artifact. Binary and useful either way — it either guts the entry's core claim (and the entry gets rescoped to within-strain, matching swab's own rule) or it hardens it. Prerequisite for any future load work, and the natural thing to do *before* the scale item, since milligrams are the only anchor that makes load genuinely cross-strain comparable — and even mg wouldn't fully do it (a gram spread thin and a gram balled up don't heat the same). **Tier:** wisdom-entry re-derivation against primary jar prose is the drift-risk class — Fable, or Opus with the adversarial-diff gate the wisdom-audit skill mandates. Not covered by the July 16 model-tier snapshot below, which predates this item. Related: **Quantify "rice grain" load descriptor**, **Load class into the verbatim record at logging**, the `load-size-effects` entry.

- **Mechanical run-facts dump script** — surfaced by the July 11, 2026 wisdom audit. Most of the evidence matrix's queryable value (dates, rigs, curves, session order, raw dab_notes/session_char text per run, all jars in one table) is mechanically derivable from the jar files with a small Python script over `load_all_jars()` — no model needed, always current, zero staleness risk. Only two layers actually needed worker judgment and don't cheaply regenerate: provenance tags (user-verbatim vs. AI-authored per claim) and the harshness-facet decomposition (onset/location/persistence/escalation split). A `dump_run_facts.py` (CSV or markdown table) would give every future session — especially cheaper ones prone to answering from HANDOFF_STATE.md summaries — a faithful, always-fresh intermediate between the summaries and 144 runs of prose, and would let the wisdom-audit skill's extraction workers focus only on the judgment layer. See `audit/matrix-2026-07-11/README.md` and the wisdom-audit skill for context.

- **Fresh-press storage — portion-and-freeze** *(July 11, 2026)*. Fresh-press rosin holds its terpene payload and its sappy texture best **frozen** — cold slows terpene off-gassing (Arrhenius: cannabinoid degradation rate roughly doubles per 10°C in a peer-reviewed resin study; volatile monoterpenes should track at least as steeply), and freezing stalls the sauce→badder nucleation that fridge/room temp lets run. The goal is to vape it **cold from frozen** to catch the full terp load, not to cold-cure it.
  - **Action:** portion the bulk into small single-/few-session amounts and freeze *those*, so the whole stash is never thermal-cycled; keep one small working portion in the fridge for hassle-free access. A pump-vac canister on the frozen portions handles oxidation but **not** thermal cycling or condensation — portioning is what handles those.
  - **Condensation:** opening a cold jar pulls humid air onto a cold surface → water in the concentrate (dab "crackle," slow quality loss). Standard advice is warm-it-sealed-15–30-min before opening, which directly conflicts with dab-it-cold — accept fast-grab-and-reseal as the cost; portioning minimizes it by keeping opens off the bulk.
  - **Reusing the empty glass jars (for the portions):** clean with **unscented** dish soap + hot water, hard rinse, then a final distilled/RO rinse to kill soap film and mineral spots. Old stuck rosin: ISO to cut it (99% flashes cleanest) — but that's a *cutting* step only, always followed by a soap wash + rinse; never the last thing to touch the glass. **Dry bone-dry** before filling (air-dry inverted or oven ~225°F/20 min) — residual moisture + frozen storage is the condensation/mold failure this whole plan is trying to avoid. De-labeling is step one of cleaning, not a separate job: a hot-water soak lifts most labels leaving no residue of its own; if adhesive remains, acetone or ISO (glass, so acetone is fine) — but any solvent used is just the first contaminant the clean-out pass then removes.
  - **Honesty flags:** the "~90% retained at fridge over 12 mo" retention numbers are industry-aggregator lore, not peer-reviewed; fridge→freezer is a *marginal* gain on top of the big room→cold win; whether cold-cure's louder nose is net terpene loss vs. structural redistribution vs. transformation is genuinely unresolved (no GC-MS in the surfaced sources); and the jar-cleaning guidance is all extractor/vendor blogs — the chemistry is sound and checkable, but no specific number is lab-verified. Researched this session via two background agents (terpene/storage physics; jar cleaning + de-labeling). Related open item: the "cold-cure continues in fridge — nose improves" wisdom row reads that nose-gain as a positive and may deserve a caveat given the unresolved-mechanism finding (session-close call).

- **Pearl-bearing Rig 6 variant — close the thermal-mass-vs-spin isolation** *(July 11, 2026)*. Add a pearl (or two) to the Rig 6 setup (sapphire insert + Wym Stick Piston, currently no pearls). By the equipment schema this is a **new `RIG_N` config**, not a mod to Rig 6 — its own constant, calibrated from baseline; log pearl runs under the new rig, never under Rig 6, or the comparison blurs.
  - **The isolation it unlocks:** the Piston is a joystick, not a vortex cap — it directs airflow but doesn't *spin* pearls. So pearls here = **thermal mass without spin**, which is exactly the "stationary-pearls-with-Piston" isolation flagged in the open *Draw velocity and cap-pearl dynamics* item above. No config in the log has separated those variables: Rig 4/5 had pearls *and* a spinner, Rig 6 has neither. This is the one setup that decouples pearl thermal mass from cap-driven spin.
  - **The live thread it addresses:** the within-draw density drop (thick-then-wispy on a single draw) appeared on Rig 5 (spinner + dual pearls) and was absent on Rig 6 for papzp22 — but the rig changed on two axes at once (pearls removed AND airflow directed). Adding pearls back to the Piston tests whether pearl thermal mass sustained density or the directed airflow did. Also bears on Rig 6's "more per draw, harshness one draw earlier" signature (does thermal mass shift onset?) and the Rig 6 endpoint map (if added mass changes effective temperature, calibration moves).
  - **Design questions to resolve first:** (a) *Physical fit* — does the Piston clear a pearl sitting in the insert cup? Bench-check before any run. (b) *Which pearl* — ruby (corundum, matches insert material, ~20x quartz conductivity — the aggressive thermal-mass test) vs. quartz; one vs. two; size. (c) *Clean comparison* — same strain, same curve, first-dab, only pearls added, against a no-pearl Rig 6 reference run on that strain. (d) *Confound to flag up front* — added thermal mass lags the programmed ascent (slower to setpoint), so "same curve" ≠ "same thermal profile"; the mass changes the ramp, not just the within-draw sustain.

- **`sessions_prior_today` vs. continuous-session order across midnight** *(Session 159, July 12, 2026)*. The stored `sessions_prior_today` counts same-`run_date` entries and is validator-locked to the calendar date, so a dab past midnight that continues the prior day's session logs as "first of the day" even when it's really the Nth consecutive dab — exactly LunarZ R3 (1:13am, fourth straight of a continuous night, logged as `sessions_prior_today=0`). Risk: a later session reads the `0`, brings the first-dab-of-day celebratory energy, and treats session order as clean when it isn't — and session order is the log's most frequent confound. **Interim handling (used this session):** the calendar count stands — don't hack the field, the validator rejects a mismatch — and the true continuous-session order goes in `session_char`/`analysis`. Open question: whether to mechanize a continuous-session-order signal (e.g. `pending_dab.py` flags when the prior run's `utc_logged_at` is within a few hours across a date boundary) or leave it judgment-layer.


- **Mechanical session-open equipment/clean reminder — strain-agnostic, prints until cleared** *(July 13, 2026)*. Requested after Sour Tangie R8 threw the heaviest reclaim the jar has logged and the user wanted a "full clean before next run" reminder that fires no matter which strain is next. The natural home I reached for — a strain's `next_text` — is strain-scoped: it only surfaces in the brief when that strain is the next dab, so it silently misses if a different jar is grabbed. Interim handling (this session): the reminder went into the "Before Next Session" list in `Dabby_Handoff_Notes.md` (a mandatory session-open read regardless of strain) — but that's judgment-layer, it leans on the assistant surfacing it, not a hard stop. Desired mechanical version: a reminder line the session-open brief (`pending_dab.py brief`) prints on every capture, independent of strain, that persists until explicitly cleared. Precedent for the mechanism: the completed **First-run-of-jar potency caution** already prints a deterministic brief line off jar state — this is the same shape, but the trigger is an ad-hoc equipment condition (dirty insert / heavy reclaim) rather than a derivable jar fact, so it needs a small piece of settable state (e.g. a reminder note in `.pending_dabs.json` or a sibling file, set via a new `pending_dab.py` subcommand and cleared on demand). Design questions: (a) where the reminder state lives and how it's set/cleared (new subcommand vs. a tiny reminders file); (b) whether it's a single global slot or a small list; (c) whether "until cleared" is manual-only or can auto-expire. Value: closes the strain-scoped gap this session exposed and makes equipment-state reminders a real stop instead of a note someone has to remember to read.

- **`curve-design-theory` entry over the per-file size warning** *(July 15, 2026; re-scoped July 16, 2026, Session 167; **valve falsified by measurement July 16, 2026, Session 168**)*. The heaviest theory entry sits at 26,631 bytes on disk against the 20,000 warn; the generator flags it every run. Exit stays 0 — this is a warn, not an error.
  - **Session 168 executed the position-side dedup valve end to end. It does not clear the warn. Do not re-attempt it as specified.** Measured harvest from distilling all seven run-narrating positions to their design-implication cores *plus* compacting the one superseded position: **5,672 bytes. The file must lose 6,632. Short by 960.** Session 167's "plausibly harvests 5–6k, clearing the warn with no split" was optimistic at both ends — even the top of that range clears by ~600 bytes with zero headroom, so the next citation appended re-trips it. The valve isn't hard to execute well; it's too small for the gap.
  - **CRLF measurement trap — cost Session 168 a wrong number.** The working tree is CRLF (384 lines) and the warn reads `os.path.getsize()` on the checked-out file, so 26,631 is the CRLF size. An LF-normalized draft measures ~320 bytes lighter than it will on disk. Session 168's first pass compared CRLF original against LF candidate and under-reported the shortfall as 636 (real: 959). Second instance of the documented "byte-comparing a Windows working copy against an LF-deployed file" failure mode in `Dabby_Handoff_Notes.md`. **Measure candidates as `len(bytes) + (lf_count − crlf_count)`, or write them CRLF.**
  - **Unresolved rule collision — needs a user ruling before position dedup is usable at all.** The warning text and the `wisdom-budget` skill name exactly two valves: split, or compact *fully-superseded* positions to git. Rewriting **current** positions is a third valve this item invented, and it contradicts the skill's hard rule: *"The entry file keeps everything; compression is about the brief, not the record."* Session 168's clean-context reviewer flagged it as off-menu, correctly. Either the rule bends (a user decision, not a drafting one) or the valve is unavailable. Note compact-to-git cannot fix this file alone regardless — the sole superseded position is ~1,050 bytes gross against a 6,632 gap.
  - **The split now looks right on the merits, not just as the last valve standing.** The Session 91 positions (shorter-and-higher; no staged ascent waypoints; fast-ramp-as-compromise) carry **zero citations**. All 13 citations are descent / bounded-hold runs from June–July 2026. Two questions, two separate evidence bases, one key — exactly what `wisdom/design/DECISIONS.md` decision 8's test asks. The entry may be failing the size budget because it is genuinely two entries, not because it is verbose.
  - **Split cost, measured July 16, 2026 (re-measure before acting — these move):** brief 35,637 / 36,000 soft cap (**363 chars headroom**); full-block LIVE 38 / 40. A two-way split takes full-block to 39 (fine) but adds one full block to the brief (~600–900 chars) — **trips the soft cap**. A split must therefore be paired with a room-vacating tier move in the same proposal, per the skill's coupling rule. Unverified candidate: `rig1-vs-rig2-gemlock` (Gemlock era ended May 2026, historical confound flag, empty `watch_for`) — nobody has approved or checked this.
  - **What held from Session 167:** positions and citation gists are confirmed NOT brief-rendered (`wisdom_core.py:57-58`; `render_brief`/`_entry_block` render claim / grade_basis / guidance / watch_for / updated and citation *counts* only). Within-file compression touches the brief by zero chars. The decoupling is real — it just isn't enough.
  - **Drift recurred under the strongest available contract — sixth logged instance.** Session 168 was a fresh, uncontaminated author, under an explicit trace-every-clause-to-source contract, actively hunting for drift — and still dropped a confound in the narrative-flattering direction: "the reclaim was insert-side residue" went out with no surviving carrier, leaving the record asserting heavy reclaim *and* no relocation to the glass with the reconciling clause gone. Two lesser defects alongside (a one-run description upgraded to an imperative design rule; "R9's driver" broadened to "the driver"). The clean-context gate caught all three; self-review caught none. **Author freshness and contract strictness do not substitute for the gate. Budget for it as the load-bearing step, every time.**
  - **Status:** open, no direction chosen — the user was presented the fork (split-paired / ratify-dedup / question the 20,000 threshold / leave it firing) and deferred. First `wisdom-budget` dogfood: the skill's *process* worked (draft → gate → triage caught a failing proposal before it shipped); the item's proposed *valve* did not.

- **Baseline-change proposal — 440-hold-descent vs. the 420 fast-ramp baseline** *(Session 157)*. The 440°F bounded-hold-descent keeps out-delivering the current baseline across strains (Sour Tangie 4-for-4; descent-curve limiting-factor pattern on four others). Case to change the project baseline is two motions, both required: **(1) Promote the pattern past directional** — Low confidence, four strains, all Rig 6, all first-dab, cross-rig untested; needs another rig and a fair session-order-matched 440-descent-vs-420-ramp head-to-head, not "it delivered." **(2) Solve the unknown-potency open** — baseline is first-contact-blind, so its opener can't be 440 on a mystery jar (first-run potency caution); a cooler-opening bounded hold might thread it, but that's a new curve to test. Related axis, same session: **load fault-tolerance** — the bounded-hold-descent absorbs load overshoot (peak heat where material's densest, cool floor where depletion lands; an over-load carries into a second cycle instead of cooking — Sour Tangie R4, one supporting run). Test as a curve-selection criterion for hard-to-portion (taffy / live-rosin) material. See the baseline-philosophy wisdom entry.

- **Track insert-clean state as per-run data** *(July 16, 2026)*. Insert cleanliness is a recurring, currently-untracked confound. Founding case: papz Run 9 — with no recorded insert state, the first-draft analysis overread a dark-gold swab spot as accumulated insert residue and framed the run's 'maybe a hint' toast note as a residue-vs-temperature question; the user had to correct it twice in conversation (insert swabbed clean after every dab, deep clean roughly weekly, spot = that dab's own material pooled outside the joystick airflow — a technique artifact). Standing practice (swab-after-every-dab) is itself a fact the log has nowhere to hold, and the trackable per-run variable is days-since-deep-clean. The same confound sits under the Sour Tangie chest thread (the R9 insert deep-clean is a live candidate for the R9–R13 chest-absence, confounded with curing) and under every deliver-vs-cook swab read. Nothing in the schema records insert state going in. Proposal — capture it per-run; options to weigh: (a) `last_deep_clean: date | None` on `CompletedRun` (lets "days since deep clean" be computed), (b) a lighter `insert_state` descriptor (deep-cleaned / swabbed-only / dirty), (c) both. Design questions: per-run field vs. device-global state fed in at capture (`pending_dab.py`), and how it renders (or whether it's analysis-only). Pairs with — but is distinct from — the open "Mechanical session-open equipment/clean reminder" item: that one nags you to clean, this one records what state you were actually in. Keeps insert-clean state out of frozen prose where it can't be queried (the fm-frozen-prose-not-promoted trap). **Reinforced July 22, 2026 (Session 174):** papzp22 R10's beige ultra-clean swab could not be credited to the new RBR top or the curve because the rig was deep-cleaned just before — another run where an untracked deep-clean confounds the swab read.

- **Consider tracking charge cycles — dabs-per-charge** *(July 16, 2026, consider)*. Track battery charge events so dabs-per-charge is computable — practical (know when a charge is due) and a slow read on battery health over time. Framed as "consider": this is a device-usage metric, not an analysis lever for the dab science — it confounds no run comparison, so it's lower-priority than the insert-clean item and shouldn't bolt onto `CompletedRun`. Mechanism to scope if pursued: a charge is an event between runs, so it wants its own capture (a `pending_dab.py`-style "charge" subcommand writing a timestamped marker, or a small charges log), then dabs-per-charge = runs between consecutive markers by `utc_logged_at`. Open questions: does the Switch² report battery level (would enable "dabs remaining," not just per-charge counts); and is per-charge count even stable enough across load size / cycle count / curve to mean anything, or too noisy to bother.

---

## Model-Tier Sort — dated snapshot (July 16, 2026, Session 166)

A ranking of every item above by which model tier actually suits it, produced when
the user asked "which items are most suitable for a Fable session" during the wisdom
migration build. **This is a snapshot tied to the Open list as it stood on the date
above — it goes stale the moment an item is added, resolved, or reworded.** A future
Claude asked to re-sort should not assume this ordering still holds; re-derive it
against the live Open list using the method below, and either replace this section or
append a new dated one (whichever keeps the file more honest about drift).

**Method.** The question is never "which model is smartest" — it's where judgment
errors are expensive and hard to catch versus where the task is a well-specified
floor. Concretely: (1) does the task require compressing or re-deriving something
where drift is easy to produce and hard to self-catch (the project's most documented
failure class)? (2) is it a design call with no existing spec — i.e. would a fresh
session have to invent the shape of the solution, not just execute one? (3) is the
task self-referential — designing a process that checks *this* project's own
processes? Any "yes" pulls toward Fable. Where the item's own text already enumerates
the design questions, options, and constraints, a senior-but-cheaper model can execute
against that spec safely — the pre-digestion is what makes it safe, not the topic.
Purely mechanical extraction, small schema additions with the questions already
listed, and skill/doc edits are Sonnet. Anything the item's own text says needs no
model ("no model needed," a script over existing data) is script-tier. Physical
actions (weighing, cleaning, freezing, asking a friend a question) aren't AI work at
any tier. A separate axis, orthogonal to all of this: several items are **trigger-gated
and not ready regardless of tier** — they wait on a recurrence, a data threshold, or
runs that don't exist yet; their tier assignment is for *when* they fire, not now.

**Fable-suitable (compression-with-drift-risk, unspecified design, or
self-referential process design):**
- **Dabby as open-source companion spec** — voice-preserving compression with no
  playbook; the failure mode (strip citations, keep unearned confidence) is exactly
  the drift class self-review misses.
- **Fix the pre-send register-leak check's syntactic blind spot** — the open question
  is whether this class of leak can be mechanized *at all*; a blinded diagnostic
  exercise is on file specifically for a fresh mind to test that question
  independently.
- **Baseline-change proposal** — *trigger-gated* (waiting on a session-order-matched
  head-to-head that doesn't exist yet). The highest-stakes methodology call in the
  project once the evidence lands — changes what every new jar defaults to, collides
  with the first-run potency-safety caution. Fable when the data exists, not before.
- **IRB sub-agent for What to Try Next review** — *trigger-gated* (deferred pending a
  recurrence of the researcher-participant conflict). Designing a process that audits
  this project's own recommendation process is the self-referential case.

**Opus-with-a-good-spec (real judgment, but bounded by constraints already stated in
the item):**
- **Workflow simplification (PR-optional logging)** — designing a "rendered-diff gate
  with teeth" against two logged silent-content-loss incidents; the safety bar is
  real even though the item scopes it tightly.
- **Build spec — session-close-handoff skill** — the checklist-wrapping spec is
  already enriched; what's left is the mechanism call (real harness gate vs. prose
  self-report) — a judgment question about tooling capability, not content.
- **Lifecycle flag on `StrainStatus`** — *trigger-gated* (needs an actual manifest-move
  miss Q7 didn't catch). When it fires: simultaneous edits to `Dabby_Core.py` and
  ~17 jar files with no test suite — wants supervision even though the shape is
  known.
- **SessionStart hook to enforce required file reads** — *dormant* (revisit only if
  skill-triggering proves unreliable in practice). The tradeoff analysis is already
  done (Session 101) — this is a re-evaluate-the-premise task, not a build.

**Sonnet-sized (spec is in the item, or the work is mechanical extraction /
small schema addition / skill-doc edit):**
`wisdom-budget` skill · `curve-design-theory` split-or-compact · CLAUDE.md User
Configuration block · Jar-return check (pick between two pre-analyzed options) ·
Check selected model at dab-skill start · Debug-deploy skill · Curve schema
Switch profile-slot color code (design questions already enumerated, device facts
already gathered) · Strain effect-character field ("yoga strain") · Load class into
the verbatim record at logging · `sessions_prior_today` vs. continuous-session order
(one narrow judgment call, not open-ended like the register-leak item) · Mechanical
session-open equipment/clean reminder · Pre-dab tasting-notes / flavor prediction ·
Visual distinction between What to Try Next and new-strain onboarding · Pearl-bearing
Rig 6 variant (the schema half, once the physical bench-check is done). Strain-to-
strain temperature variance on Rig 6 is a methodology conversation with the user, not
a build — any capable model with the wisdom layer loaded handles it credibly.

**Script, not a session:**
- **Mechanical run-facts dump script** — the item's own text says "no model needed."

**Not AI work at any tier (physical, or someone else's action):**
- Session date backfill (user's memory) · Quantify "rice grain" load (a scale) ·
  Control water temperature and change frequency (practice change) · Dab rig vs.
  erig temperature ranges — Oliver consult (a friend) · Percolation glass as a
  testable harshness variable (glassware + a run) · Fresh-press storage
  portion-and-freeze (a freezer) · Draw velocity and cap-pearl dynamics (waiting on
  a deliberate isolation run, not a session).

**Headline, dated:** after the wisdom migration, the backlog is thin on Fable-shaped
work — only the companion spec is genuinely ready for it today; the other three
Fable-tier items are trigger-gated. Most of what remains is well-specified floor work
because prior sessions already did the design thinking and left it in the item text —
which is itself the thing to preserve when adding new items: a well-specified item is
a gift to whichever model tier eventually picks it up.

---

## Completed

- ~~**`wisdom-budget` skill — wire the budget warnings to their designed response**~~ — **COMPLETED July 16, 2026 (Session 167).** `.claude/skills/wisdom-budget/SKILL.md` authored (Sonnet build, orchestrator review); all three warning strings in `wisdom_core.py` / `wisdom/manifest.py` now name the skill. Review caught and fixed two defects pre-merge: a misattributed design-decision citation baked into the worked example (invented-specificity class), and the missing valve-coupling rule (a split raises the full-block count and grows the brief — the skill now requires computing where a split lands the other two budgets before proposing it). Dogfood test deliberately deferred to a fresh session; see the re-scoped `curve-design-theory` item above.
- ~~**Mechanical run-facts dump script**~~ — **COMPLETED July 16, 2026 (Session 167).** `dump_run_facts.py` (Sonnet build): `run_facts/<slug>.md` per jar + `index.csv`, gitignored, on-demand, provenance-tagged (USER-VERBATIM / EXTRACTED / AI-AUTHORED / SUPERSEDED), line anchors via AST, field map asserted against the dataclass at import so schema drift fails loud. Tested against precomputed SHA fingerprints: all 157 `dab_notes` byte-identical, zero missing/extra; six load-coverage numbers re-derived from the dump matched the jar-file oracle exactly.
- ~~**Visual overhaul of the log**~~ — **COMPLETED Session 113.** New designer CSS; forest green retired.
- ~~**Rolling run archive — Phase 2**~~ — **SUPERSEDED Session 108** by per-jar architecture. Single-jar scaling deferred (revisit if a jar approaches the read-tool cap).
- ~~**Dashboard: style Next pill in strain accent color**~~ — **SUPERSEDED Session 113.** Redesign retired pill/badge style.
- ~~**Resolve bp4rw13 Runs 5–8 equipment field**~~ — **RESOLVED July 2, 2026.** Seven runs across two jars (WM R16–18, bp4rw13 R5–R8) corrected to Rig 6; stale jar-local defaults after 14–15-day gaps.
- ~~**Move dab-of-the-day counting to dab skill**~~ — **RESOLVED July 3, 2026 (Session 144).** `pending_dab.py brief`/`consume` compute and print the count.
- ~~**HANDOFF_WISDOM.md exceeds the single-pass Read cap again**~~ — **RESOLVED July 15, 2026 (wisdom-as-data migration).** The layer is typed entries under `wisdom/entries/` with a generated, hard-capped `WISDOM_BRIEF.md` as the mandatory read (~33k chars, one Read); regrowth of the unconditional surface now fails the generator and CI. Design record: `wisdom/design/DECISIONS.md`; the item's regrowth question is answered structurally per mechanism in `wisdom/design/accepted-proposal.md` §7.
- ~~**HANDOFF_WISDOM.md consolidation pass ("pass 3")**~~ — **COMPLETED July 3, 2026 (Session 146).** 90.9k → ~63.3k chars; `citecheck.py` clean (220/220).
- ~~**Adjust dab/log-run skills for register leaks**~~ — **RESOLVED July 3, 2026 (Session 144).** `pending_dab.py` prints display-form facts; skills compose from that output with pre-send checks.
- ~~**Analysis-toolkit skill**~~ — **COMPLETED July 4, 2026 (Session 147).** Ten recipes in `.claude/skills/analysis-toolkit/SKILL.md`; three Claude failure modes migrated to Structurally Resolved.
- ~~**Predict-before-running and refute-before-promoting**~~ — **SHIPPED July 3, 2026 (Session 146).** Expected/surprising pair in AI Analysis; counter-reading required for confidence promotions.
- ~~**Project audit**~~ — **COMPLETED July 3, 2026.** `AUDIT.md` and `citecheck.py` merged; findings await user triage.
- ~~**First-run-of-jar potency caution**~~ — **RESOLVED July 10, 2026.** Four layers, evaluated before building: mechanical caution line in `pending_dab.py brief` for any zero-run jar (fires deterministically off `RUNS`, covers the motivating dbrb R1 path); best-effort name match in party-mode capture so the warning lands before the dab, not at reconciliation; dab skill re-runs the brief when a strain is named after a vague open (closes the judgment-layer gap); new-jar skill writes the caution into every new jar's What to Try Next (pattern set by the Sour Tangie/LunarZ jars, PR #236). Protocol line in `Dabby_Handoff_Notes.md` cites both instances (papzp22 R1, dbrb R1). The related jar-return check item remains open — same shape, different trigger.
- ~~**Curve table in the dab brief renders wonky on phone displays**~~ — **RESOLVED July 6, 2026.** Chat curve format locked after side-by-side mobile comparison: a markdown table (bolded temp column, time column), emitted directly by `fmt_curve_table()` in `Dabby_Core.py` so skills paste it verbatim instead of reformatting — the table renderer enforces alignment, closing the drift that prose instructions didn't hold. The fenced-block first-line whitespace bug is moot under this format (no fence, no leading whitespace). Full history in git.
